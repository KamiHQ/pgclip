<%
require 'csv'
lines = []
lines.append(CSV.generate_line(@fields, force_quotes: true))
@values.each do |value|
  lines << CSV.generate_line(value, force_quotes: true)
end
lines
%>
<% lines.each do |line| %><%= line.html_safe %><% end %>
